// import PropTypes from 'prop-types';
// // impt 치면 자동 생성

// const GNB = () => {
//   return <div></div>;
// };

// export default GNB;

// // 코드 스니펫 사용 > rafc 등이 있음

// setTask(tasks.filter(task) => task.id !== id)
